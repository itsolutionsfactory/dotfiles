version: '3.8'

services:
  dotfiles-test:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dotfiles-test-1
    volumes:
      - .:/home/testuser/dotfiles
    environment:
      - TERM=xterm-256color
    command: /bin/zsh
    tty: true
    stdin_open: true 